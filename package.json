{
  "name": "schemaforge",
  "version": "1.0.0",
  "description": "Generate OpenAPI schemas from code automatically",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "nuxt": "^3.15.0",
    "js-yaml": "^4.1.0"
  }
}
